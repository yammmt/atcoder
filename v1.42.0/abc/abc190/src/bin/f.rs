// use petgraph::unionfind::UnionFind;
use proconio::input;
// use proconio::marker::Chars;
// use std::collections::HashSet;
// use std::collections::HashMap;
// use std::collections::VecDeque;
// use permutohedron::heap_recursive;

// 愚直では O(n^2) TLE
// 以前のセグ木のように知っていれば解ける雰囲気 BIT?

fn main() {
    input! {
        n: usize,
        an: [i64; n - 1],
    }
}
