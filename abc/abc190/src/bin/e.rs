use petgraph::unionfind::UnionFind;
use proconio::input;
// use proconio::marker::Chars;
// use std::collections::HashSet;
// use std::collections::HashMap;
// use std::collections::VecDeque;
// use permutohedron::heap_recursive;

// 作れるか否かは UnionFind のグループが同じか
// 個数最小値 (17) は階乗で全探索は無理だが bit 全探索はできる 巡回セールスマン問題っぽい
// 二点間の距離は BFS すればよいが同一グループ内の点数に影響され結局 TLE ではと思ったがそうでもなかった
// 重み付き UnionFind?

fn main() {
    input! {
    }
}
