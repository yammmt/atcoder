// セグ木か BIT, 後回し

use proconio::input;
// use std::cmp::Order;

fn main() {
    input! {
        // n: usize,
        // q: usize,
        // tkq: [(usize, usize); q],
    }

    // 愚直に反転し続けるでは O(N^2)
    // 反転条件をいもす法でも O(N^2)

    // for tk in tkq {
    //     let t = tk.0;
    //     let k = tk.1;
    //     match k {
    //         1 => {
    //         }
    //         2 => {
    //         }
    //         _ => unreachable!(),
    //     }
    // }
}
